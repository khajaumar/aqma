//const bigNumberMultiple =( num1 ,num2 ,num3 ,num4) =>{
   // let Bignum=0;

   //// Bignum = num1>num2 ? num1 : num2;

   // Bignum = Bignum>num3 ? Bignum : num3;

    //Bignum = Bignum>num4 ? Bignum : num4;
   // return Bignum;
//};

//console.log(`Bigger number is ${bigNumberMultiple(23, 56 ,75 ,21)}`);

//const bigNumberMultiple =( num1 ,num2 ,num3 ,num4) =>
   //Math.max(num1, num2, num3, num4);
const maxNumber=(num1, num2, num3, num4 )=>Math.max(num1, num2, num3, num4);

console.log(`Bigger number is  ${maxNumber(23, 56 ,75 ,21)}`);